{
  "name": "hydration_nodehono",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "file": "pnpm --filter @hydration_nodehono/core run file",
    "devcore": "pnpm --filter @hydration_nodehono/core run dev",
    "build": "pnpm vite build && vite build --mode client",
    "deploy": "pnpm build && wrangler pages deploy dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^1.4.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.4",
    "vite-node": "^0.34.6",
    "wrangler": "^3.17.1"
  },
  "dependencies": {
    "hono": "^3.9.0"
  }
}
